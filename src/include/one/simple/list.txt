types -> GOOD

defines -> GOOD

memory ops -> GOOD
     copying w/ (overlap support), 
     comparing,
     painting (initializing),
     scrubing (hard initializing),
     xoring
               
allocation ops -> GOOD
     malloc/free

string library -> GOOD
